# toy_ufdb ロードマップ

Union-Find（素集合データ構造）を核にしたトイDB。Rust製、CLI(REPL)、キーのみ管理。

## バージョニング方針

メジャーバージョンごとにリポジトリを分ける、という方針自体は維持する。ただし**ストレージ（永続化）・TCPサーバー化・ユーザーアカウント（TCP接続時のログイン用）・configファイル**の4機能は、当初v1（別リポジトリ）を想定していたが、v0（このリポジトリ）に前倒しして実装することに変更した（Phase 12〜15）。

- **v0（このリポジトリ）**: オンメモリのUnion-Find/DB機能に加え、上記4機能を含む
- **v1（別リポジトリ）**: v0からさらに先の大きな方針転換が必要になった場合に検討する（現時点で具体的な内容は未定）

このロードマップの「Phase」は基本的にv0（このリポジトリ）のスコープ。

## Phase 0: プロジェクト初期化
- [x] `cargo init --name toy_ufdb`（バイナリクレート）
- [x] `src/lib.rs`（コアロジック）と `src/main.rs`（REPL）に分割する構成にする
- [x] `.gitignore` に `/target` を追加

## Phase 1: Union-Findコア (`src/lib.rs` or `src/union_find.rs`)
データ構造:
- `HashMap<String, usize>` … キー → 内部インデックス
- `Vec<usize>` parent配列
- `Vec<usize>` rank または size配列（union by rank/size用）

実装する操作:
- `make_set(&mut self, key: &str)` … 未登録キーなら新しいグループとして追加。既存なら何もしない
- `find(&mut self, key: &str) -> Option<String>` … 代表元のキーを返す。path compression込み。存在しないキーは `None`
- `union(&mut self, a: &str, b: &str)` … 2つのグループを統合。存在しないキーは内部で `make_set` して扱う
- `same(&mut self, a: &str, b: &str) -> bool` … 同じグループか判定。`UnionFind::same` と名前を揃える。`unite` と異なり、未登録キーが渡されても `make_set` はせず、単に `false` を返す（SAMEは参照系の操作であり、呼ぶだけでデータが増えるのは直感に反するため）

設計判断ポイント（実装しながら決める）:
- ~~`find` の再帰 vs ループ実装（再帰は大量データでスタックオーバーフローの可能性あり → ループ推奨）~~ → union by sizeで木の高さがO(log n)に抑えられるため、再帰のままで問題なしと結論
- union by rank と union by size のどちらにするか → **決定: size**（後の「グループ一覧・グループサイズ」機能と相性が良い）
- 存在しないキーに対する挙動 → **決定**: `unite` は自動登録（`make_set`してから統合）、`connected` は登録せず `false`

単体テストで最低限カバーすべきケース:
- 単純な union → find で同じ代表元になる
- 推移的な union（a-b, b-c → a と c が connected）
- 未登録キーの find/connected
- 同じキーを2回 union しても壊れない（冪等性）
- path compression 後も find の結果が変わらないこと

## Phase 2: REPL / CLI (`src/main.rs`)
- [x] `Cargo.toml` に `clap`（`features = ["derive"]`）を追加
- [x] `Cli` / `Commands` の型定義（`#[command(rename_all = "UPPER")]` でサブコマンド名を大文字化）
- [x] REPLループ本体: 標準入力を`for line in io::stdin().lock().lines()`で1行ずつ読み、空白分割した引数の先頭にダミーのプログラム名（`"repl"`）を足してから`Cli::try_parse_from(...)`に渡す
  - パースエラー（`Err`）は`eprintln!`でそのまま表示（パニックさせない）。`help`/`--help`もclapが`Err`（`DisplayHelp`）として返すため、この分岐がそのまま兼ねている
- [x] `INSERT <key>` を `Ufdb::make_set` に接続し、実際にデータ投入できることを確認
- [x] `EXIT` でループを`break`して終了することを確認
- [x] 各コマンド実行のたびに実行時間を表示する
  - `std::time::Instant::now()` で計測開始 → コマンド実行 → `elapsed().as_secs_f64() * 1000.0` をミリ秒として`{:.6}`（小数点以下6桁）で表示
  - 内部の計測精度（ナノ秒）は変えず、表示単位だけをms・小数表記にしている（例: `0.001500ms`）
  - `EXIT`は`break`で即ループを抜けるため、この表示は自動的にスキップされる
- [x] `MERGE`/`SAME`/`GROUPS` を `Commands` に追加し、`Ufdb`の対応メソッドに接続する
- 補足: 現状の`Commands`には動作確認用に追加した`Hello`（挨拶を表示するだけのダミーコマンド）が残っている。正式なコマンド一覧（README参照）には含まれないため、削除待ち（他コマンドは揃った）
- `FIND`（代表元キーをそのまま返すコマンド）は採用しない。代表元はunion by sizeの内部ロジック次第でユーザーからは予測できない値になるため、コマンドとして見せる価値が薄いと判断（詳細はREADME参照）。`find` 自体は `unite`/`same`/`GROUPS` の内部実装としては引き続き使う
- [ ] プロンプト表示: 各行の読み込み前に現在選択中のDB名を含むプロンプト（例: `{db_name}>> `）を表示する。DB名は`Db`が保持する`current_db`（`src/db.rs`）から取得する。`CREATEDB`/`USE`直後の次回プロンプトから即座に反映されることを確認する
- [ ] コマンド履歴: 上矢印キーで直前に入力したコマンドを呼び出せるようにする。現状の`io::stdin().lock().lines()`による素朴な行読み込みでは矢印キーのエスケープシーケンスを扱えないため、`rustyline`など行編集・履歴機能を持つクレートの導入が前提になる（新規依存）。既存の`Cli::try_parse_from`へのライン渡し方に影響しないか要確認

## Phase 3: グループ機能の仕上げ
- [x] `Ufdb::groups()`: `HashMap<代表元, Vec<キー>>`（実際は`HashMap<usize, Vec<&String>>`）を組み立てる集計ロジックを実装
- [x] `GROUPS` コマンドの表示形式: 代表元・連番ラベルなしで1行1グループにメンバーを列挙する形で実装した。`main.rs`側で`ufdb.groups()`の結果を`into_values()`でVecに変換し、各グループ内をキー昇順、グループ同士をサイズ降順にソートしてから`join(", ")`で1行にまとめて表示する
- 残課題: サイズが同点のグループ同士のタイブレーク方法（未定。例: ソート後の先頭メンバーキーで比較するなど。今のところ`sort_by`が安定ソートなので`into_values()`のHashMap由来の順序がそのまま残るが、これは決定的ではない）
- [x] `SIZE <key>`: キーが属するグループのサイズを表示。`Ufdb::size(key) -> Option<usize>`で橋渡しし、`main.rs`の`Commands::Size`で`Some`なら標準出力、`None`（未登録キー）なら標準エラーにメッセージを出す形で実装した

## 開発用コマンド: SEED（固定ダミーデータの一括投入）
毎回手でINSERT/MERGEを打ち込むのがしんどいので、起動直後に1コマンドで固定のダミーデータを投入できるようにする。REPLの通常コマンドの1つ（`SEED`）として実装し、実行後もそのままREPLでの対話操作を続けられる（Phase 7のCSV importとは別物: importは外部ファイルをリダイレクトで流し込む用途、`SEED`はアプリ内蔵の固定データをその場で投入する用途）。

- [x] `SEED`コマンド: あらかじめコード内に固定で用意したキー・MERGE操作の組を`Commands`に追加し、`Ufdb`に対して順番に`make_set`/`unite`を実行する
- [x] 中身は単なるフラットなキーの羅列ではなく、いくつかのMERGE済みグループ（大小混ぜる）も含める（`apple-banana-cherry`/`date-elderberry`/`grape-honeydew-kiwi-lemon`/単独の`fig`）。SEED直後に`SAME`/`GROUPS`の見た目をすぐ確認できる
- [x] `Ufdb::is_empty()`で既存データの有無を判定し、既にキーが存在する場合はREPL側（`main.rs`）でy/n確認を挟んでから`seed()`を呼ぶ。確認待ちの追加入力を読むため、REPLループを`for line in ...`から`let mut lines = ...; while let Some(line) = lines.next()`という明示的なイテレータ変数に持ち替えた
- 位置づけ: DB本体の機能ではなく開発・動作確認用のショートカット。パラメータは持たず、常に同じ固定データを投入する（乱数生成や件数指定はしない）
- 補足: `Commands`列挙子の名前は`SEED`のまま（`clippy`の`upper_case_acronyms`警告あり、`Seed`へのリネームは他コマンドと合わせるなら検討の余地あり）

## Phase 4: テスト・仕上げ
- [ ] `tests/` 以下に統合テスト（ライブラリAPIを直接呼ぶ形でOK）
- [ ] `cargo clippy` / `cargo fmt` を通す
- [ ] README に使い方（コマンド例）を記載
- [ ] 10万件（`N = 100,000`）規模のデータを生成し、`toy_ufdb < data.txt` のようにリダイレクトで流し込んで動作確認する（手入力ではなく生成データで検証）

## 既知の制約（v0＝このリポジトリではやらない・やれないこと）
- **削除・分割は直接サポートしない**: 古典的Union-Findは一度unionしたグループを分割する操作をサポートしない。Phase 6で「union履歴を保持しての組み直し（rebuild）」というワークアラウンドを実装する予定（詳細はPhase 6参照）。
- ~~永続化なし: プロセスを終了するとデータは消える。v1（別リポジトリ）で対応する。~~ → Phase 12でv0に前倒しして対応する
- **値（value）なし**: 要素はキーのみ。KVとして使いたくなったら拡張が必要（v0/v1どちらでやるかは未定）。
- **規模**: 動作確認は10万件（`N = 100,000`）規模まで。理論上はメモリが許す限り動作するはず（union by size + 経路圧縮により操作はならしO(α(n))で、キー数に対してほぼ線形にしかメモリを消費しないため）だが、それ以上の規模は未検証。

## Phase 5: DB / GROUP（2層構成にする）
`DB`（名前付きの`Ufdb`インスタンス）→ `GROUP`（代表元ごとに自動形成されるグループ。ユーザーが直接作るものではない）という2層構成にする。`USE`で切り替えるのは`DB`のみ。

- [x] データ構造: `HashMap<String, Ufdb>`（DB名 → `Ufdb`）を持つ層を、`union_find.rs`→`Ufdb`と同じレイヤリングで新規モジュール`src/db.rs`（`Db`構造体）に切り出した。フィールドは`current_db: String`（現在選択中のDB名）と`db: HashMap<String, Ufdb>`の2つ。`current(&mut self) -> &mut Ufdb`で選択中の`Ufdb`への可変参照を返す（`current_db`は常に`db`内の実在するキーを指す、という不変条件を`new`/`create_db`/`use_db`が維持する）
- [x] `CREATEDB <name>` コマンド: `Db::create_db(&mut self, name: &str) -> bool`。`db`に`name`が無ければ`Ufdb::new()`を挿入、あれば何もしない。**いずれの場合も`current_db`を`name`に切り替える**（新規/既存で非対称にしない）
- [x] `USE <name>` コマンド: `Db::use_db(&mut self, name: &str) -> bool`。`db`に`name`が存在すれば`current_db`を切り替えて`true`、存在しなければ**切り替えずに`false`**を返す（自動作成はしない）。`main.rs`側は`false`が返ってきたときだけ「DBが存在しません。作成しますか？(y/n)」と確認し、`y`なら`create_db`を呼ぶ（`SEED`の既存データ確認プロンプトと同じ役割分担: 存在判定と本来の切り替えは`Db`、ユーザーとの対話は`main.rs`）。当初想定していた「即エラー」より一段階親切な形に着地した
- [x] 起動時に名前付きのデフォルトDB（`ufdb`）を1つ用意し、最初からそれが選択された状態にしておく（PostgreSQLの`postgres`データベースに相当。`Db::new()`が`"ufdb"`キーで`Ufdb::new()`を1つ持った状態で初期化する）
- ~~設計判断ポイント: `USE`で存在しない名前を指定されたときに自動作成する（`make_set`と同じ自動登録方式）か、`CREATEDB`のような明示的な作成コマンドを別途用意するかを実装時に決める~~ → **決定**: 明示的な入力を優先し、`CREATEDB`を新設。`USE`は自動作成しない
- ~~設計判断ポイント（実装しながら決める）: `CREATEDB`に既存名を指定した場合の挙動（`make_set`のようにbool返却でno-opにするか、エラーにするか）~~ → **決定**: エラーにはせず、常にそのDBへ切り替える（＝`CREATEDB`は「新規なら作成」＋「常にUSEを兼ねる」の2ステップ。新規作成時も既存名指定時も、実行後は必ずそのDBが選択された状態になる。挙動が新規/既存で非対称にならない）
- 位置づけ: この機能はPhase 12（ストレージ／永続化）より先に実装する。名前付きの入れ物（DB）を先に用意しておくことで、後続の永続化は「1DB = 1ファイル」のように素直に対応させられる
- **DBは互いに完全に独立**: 各DBは別々の`Ufdb`インスタンス（独自の`keys`・独自の`UnionFind`）なので、DBをまたいだ`MERGE`/`SAME`などは成立しない。UFQLのコマンドは常に「現在`USE`で選択中の1つのDB」だけを操作対象にする。DB間でデータを行き来させたい場合は、対象DBに`USE`で切り替えた上で改めて`INSERT`/`MERGE`し直す必要がある（自動的な相互参照・インポート機構はv0では持たない）
- プロセス自体がDBより上位の概念（複数DBを保持する暗黙の入れ物）であり、これ自体に名前を付けたり切り替えたりする必要はない（MySQLサーバー自体に相当する）

## Phase 6: 削除サポート（graphの再構築によるrebuild）
古典的Union-Findは分割・削除を直接サポートしないため、「実際にグループを統合した辺だけを持つ`graph`（Phase 11参照）から対象を除いて、`UnionFind`を一から組み直す」というワークアラウンドで対応する。

- [x] `graph`はPhase 11で`Ufdb`に追加する`HashMap<String, Vec<String>>`（実際に統合された辺のみを持つ全域木/森）をそのまま流用する。Phase 6独自の履歴構造は持たない
- [x] 削除実行時: 対象キー（またはUNMERGE対象の辺）を`graph`から取り除き、残った辺で新しい`UnionFind`を一から組み直す（`add`→`unite`を辺の集合に対して実行）。`graph`が閉路のない全域木/森であることが保証されているため、削除は「対象キーに触れる辺を除く」だけでよく、元の時系列順に再生し直す必要はない（連結性の最終結果は辺を足す順序に依存しないため）
  - 実装: `Graph::remove_edge`（`src/graph.rs`）と`Ufdb::unmerge`（`src/lib.rs`）。`UNMERGE <a> <b>`コマンドとして`main.rs`のREPLにも接続済み
  - `DELETE <key>`（キー削除）は引き続き未着手。インデックス振り直しの設計判断が必要なため、決定事項どおりスコープ外のまま
- コスト: 削除1回あたり **O(E・α(E))**（`E`は`graph`が保持している辺の数、`α`は経路圧縮+union by sizeでのならし計算量）を許容する。頻繁な削除がボトルネックになるようなら、より効率的な削除可能データ構造への置き換えは将来の検討課題（v1以降）
- ~~設計判断ポイント（実装しながら決める）: 「削除」の対象は何か: 特定のキーをデータベースから完全に削除する（`DELETE <key>`）のか、特定の辺（過去の`MERGE`）だけを取り消す（`UNMERGE <a> <b>`的なもの）のか。`graph`が全域木/森になったことで、後者（特定の辺の削除＝木の枝刈り）も素直に扱えるようになった~~ → **決定**: `UNMERGE <a> <b>`（辺の削除）から着手する。`DELETE <key>`はキー削除後に`UnionFind`のインデックス（`Vec<usize>`、連番前提）へ穴を残すか全キーを振り直すかという追加の設計判断が必要で難易度が高い。`UNMERGE`はキーの集合（universe）自体は変わらないため、`graph`から対象の辺を1本除いて残りの辺で`UnionFind`を組み直すだけで済み、インデックスの振り直し問題が発生しない
- Phase 5でDBが複数になったことで、`graph`は`Ufdb`ごと（＝DBごと）に独立して持つ状態が既に実現している（`Db`が`HashMap<String, Ufdb>`で各`Ufdb`を保持しており、`graph`は`Ufdb`のフィールドのため自動的にDB単位で分離される）

## Phase 7: CSV import / export
アプリ自身はファイルを直接開かず、既存の標準入出力ベースのREPLをそのまま使う。ファイルの読み書きはシェルのリダイレクトに任せる（Phase 12のストレージ機構とは別経路）。

- [ ] **import**: 標準入力から読む行がCSV形式でも解釈できるようにする（目的はシードデータの一括投入）。`toy_ufdb < seed.csv` のようにリダイレクトして使う
- [ ] **export**: `GROUPS` の出力をCSV形式で出せるようにする。ファイルに保存したい場合は利用者がシェル側で `> out.csv` のようにリダイレクトする（アプリはファイルを意識しない）
- 設計判断ポイント（実装しながら決める）:
  - CSVの1行をどのコマンドとして解釈するか（例: 1列なら`INSERT`相当、2列なら`MERGE`相当、など）
  - ヘッダー行の有無、区切り文字、クォート処理をどこまで真面目に扱うか（自前パースで十分か、`csv`クレートのような専用ライブラリを使うか）
  - `GROUPS`のCSV出力フォーマット（代表元列＋メンバー列、1メンバー1行、など）

## Phase 8: EXPLAIN（dry run）
SQLの`EXPLAIN`にならい、コマンドを実際には実行せず、内部で何が起きるかを見せる`EXPLAIN <command>`を追加する。

- [ ] `UnionFind`に経路圧縮を行わない読み取り専用の`find_readonly(&self, x: usize) -> usize`を追加する（`&self`を取ることで「状態を一切変更しない」ことを型レベルで保証する。既存の`find`は経路圧縮ありの`&mut self`のまま残す）
- [ ] `EXPLAIN SAME a b` / `EXPLAIN MERGE a b`: `find_readonly`で両者の代表元とグループサイズを求め、一致するか（`MERGE`なら union by size でどちらが親になるか、すでに同じグループならno-opになる旨）を表示する
- [ ] `EXPLAIN INSERT key`: 既存キーかどうか（`make_set`が`true`/`false`のどちらになるか）を表示する
- [ ] `EXPLAIN GROUPS`: 見せられる情報は少ない（全キーを走査するだけ）。実装するか自体を検討する
- 設計判断ポイント: `find_readonly`は経路圧縮をしないため、その場では通常の`find`より遅くなる（木の高さ分かかる）。EXPLAINは低頻度の診断用コマンドなので許容する

## Phase 9: STATS（統計情報）
既存のデータ（`keys`と`UnionFind`の`parent`/`size`）だけで計算できる、DB全体のサマリを表示するコマンド。どの項目を採用するかは未定（候補を列挙しておき、後で絞る）。

候補:
- 登録キー数（`N = keys.len()`）
- グループ数（代表元の種類数。`GROUPS`と同じ走査で数えられる）
- 最大/最小/平均グループサイズ（`GROUPS`の結果から集計）
- 最大木の高さ（`find_readonly`（Phase 8）を使えば副作用なく実際の深さを測れる）
- Phase 6（削除サポート）実装後なら、保持しているunion履歴の件数

- 実装方針: `GROUPS`の集計ロジックを再利用してサマリだけ出す形が自然
- 位置づけ: v0スコープ（既存のオンメモリデータからの計算のみ、ストレージ不要）

## Phase 10: コマンドごとのメモリ使用量表示（カスタムアロケータ）
実行時間の表示と対になる機能。各コマンド実行の前後で、実際に確保・解放されたバイト数を表示する。

- [ ] `#[global_allocator]` で `System` アロケータをラップした独自アロケータを定義する（`GlobalAlloc`トレートを実装）
- [ ] `alloc`/`dealloc` の中で `AtomicUsize` のカウンタを加減算し、プロセス全体のアロケーション量を追跡する
- [ ] 各コマンド実行の前後でこのカウンタを読み取り、差分をナノ秒の実行時間表示と並べて表示する
- 注意点: これは**プロセス全体**のアロケーション活動を捉える。`println!`のフォーマットや`clap`のパースなど、`Ufdb`本体とは無関係な一時アロケーションのノイズも混ざる（実行時間表示における「OSタイマー精度は実際はナノ秒より粗い」という注意書きと同様の位置づけ）
- 設計判断ポイント（実装しながら決める）:
  - 表示するのは「正味の増減（確保 − 解放）」か、「確保したバイト数の総量（解放は無視）」か。一時的なVecなどはコマンド内で確保後すぐ解放されることもあるため、正味の増減だけだとほぼ0に見えるケースがある。何を見せたいかで設計が変わる

## Phase 11: HTML出力（静的なMERGE構造の一覧表示）
現状の`UnionFind`は経路圧縮によって`parent`が平坦化されるため、`parent`配列をそのまま辿ってもMERGEの実行履歴（どのキーとどのキーが直接繋がれたか）は復元できない。可視化用に、`UnionFind`の性能用データ（`parent`/`size`）とは別に「実際にグループを統合した辺」だけを保持する`graph`を`Ufdb`に追加する。この`graph`はPhase 6（削除）とも共有する（詳細はPhase 6参照）。

このリポジトリ（v0）で扱うのは静的なスナップショット出力のみ。動的な更新・編集・インタラクションはTauriアプリ側（別リポジトリ）の責務とし、ここでは実装しない。

- [x] `Ufdb`に`graph: HashMap<String, Vec<String>>`（キー→隣接キー一覧）を追加する。無向グラフとして両方向（a→b, b→a）を記録する
  - 実装は`src/graph.rs`に`Graph`構造体（`edges: HashMap<String, Vec<String>>`を1フィールドだけ持つラッパー）として切り出し、`Ufdb`は`graph: graph::Graph`を持つ形にした。`UnionFind`が`union_find.rs`にモジュール分離されているのと同じ扱い（別クレートにはしない。理由: `graph`は`Ufdb::unite`の履歴に依存するデータであり、`UnionFind`から独立していても`Ufdb`からは独立していないため）
- [x] `unite(key_a, key_b)`が呼ばれたとき、**戻り値が`true`（実際に2つのグループを統合できた）場合にのみ**`graph`にa-bの辺を追加する。すでに同じグループの場合（no-op、`false`）は辺を追加しない
  - これにより`graph`は常に閉路のない全域木/森（spanning tree/forest、重みなしなので「最小」ではない単純な全域木）になることが保証される。同じペアに複数回`MERGE`しても2回目以降はno-opなので辺が重複することもない
  - [x] 孤立ノード対応: `make_set`が新規キーを登録した分岐で`Graph::add_node(key)`を呼び、`graph`に空のVecのエントリを先に作っておく方式にした。`add_node`は`or_insert_with`で「無ければ作る・あれば何もしない」ため、その後の`add_edge`（`entry().or_insert_with(Vec::new).push(...)`）と衝突しない
- 残課題: `graph`（`Graph`の中身）を検証する結合テストは`main.rs`側にまだ書けない。理由は、現状のREPLに`graph`の中身を出力するコマンドが存在しないため（`GROUPS`はグループの中身は見せるが辺の情報は見せない）。SNAPSHOT（本Phase）か、何らかのSTATS/DEBUG系コマンドが`graph`を観測可能にしたタイミングで、`main.rs`側に結合テストを追加することを検討する。それまでは`Graph`単体のテスト（`src/graph.rs`内、`add_node`/`add_edge`の直接呼び出し）でカバーする
  - 考え方はKruskal法の閉路検出（`union-find.md`参照）と同じ: 「すでに繋がっている同士の辺は採用しない」ことで全域木を保つ
- ~~グラフの座標計算・SVG描画・大規模GROUPの閾値判定は行わない~~ → **方針転換（決定）**: 本命の可視化はTauri/React（別リポジトリ）に譲る。このリポジトリ（v0）でレイアウト計算まで作り込むのは二重投資と判断し、SNAPSHOTは大幅にスコープダウンする
- [x] `SNAPSHOT`: 現在の`graph`・`GROUPS`の状態をもとに静的なHTML文字列を組み立てる
  - **表示形式（決定）**: SVGでの座標計算はせず、各GROUPを`graph`のDFSで辿った**入れ子の`<ul><li>`リスト**として表示する（インデントで「誰と誰が直接MERGEされたか」という木構造が視覚的にわかる）
  - これにより`GROUPS`/CSV export（フラットなメンバー一覧のみ）との差別化になる。SNAPSHOTだけが`graph`の辺（実際のMERGE構造）を人間に見せる、という役割分担にする
  - 座標計算・レイアウトエンジンが不要なため実装コストは低い。`graph`が閉路のない全域木/森であることが保証されているので、DFSは各ノードを重複訪問せずに済む
  - **大規模GROUP対応（決定）**: サイズによる閾値判定はせず、**全GROUPを`<details>/<summary>`で初期状態は折りたたみ**にする（クリックで展開）。閾値の線引きが不要になり実装がシンプルになる
- **出力方式: サーバー起動+ブラウザ自動オープン（Storybook的な体験）**
  - `SNAPSHOT`はstdoutには出さず、常にローカルサーバーを起動してHTMLを配信し、`open`クレートでデフォルトブラウザにそのURLを開かせる
  - サーバーの生存期間は**1リクエスト分だけ**: GETリクエストを1回受けてHTMLを返したらリスナーを閉じ、REPLに制御を戻す（バックグラウンドで生かし続けない。マルチスレッドや`Ufdb`の共有状態管理が不要になり実装がシンプル）。最新化したい場合は改めて`SNAPSHOT`を呼び直す運用にする
  - 実装は`tiny_http`（軽量な同期HTTPサーバー、asyncランタイム不要）+ `open`（クロスプラットフォームでブラウザ起動）を使う。生の`TcpListener`でHTTPを手書きする案やactix/axumのようなフルフレームワークは見送る
  - これはPhase 7のCSV export（標準出力のみ、ファイルI/Oなし）とも、Phase 12のストレージ（ファイルへの永続化）とも異なる出力経路。ディスクに何も書き込まない・プロセス終了とともに消える一時的なTCPリスナーである点は変わらない
  - 新規依存として`Cargo.toml`に`tiny_http`・`open`を追加する
- ~~コマンド名（当初`EXPORT HTML`仮称）は未確定のまま~~ → **決定**: `SNAPSHOT`（1単語）に変更。他のコマンド（`INSERT`/`MERGE`/`SAME`/`GROUPS`/`SIZE`/`CREATEDB`/`USE`/`UNMERGE`/`SEED`/`EXIT`）が全て1単語で、`clap`の`rename_all = "UPPER"`もenumバリアント名（1単語）をそのままサブコマンド名にする仕組みのため、2語コマンドは一貫性・実装の単純さの両面で不利と判断。名前自体も「静的スナップショットのみを扱う」というPhase 11の設計意図と合っている
- Phase 6（削除）とはこの`graph`を共有する: 「実際にグループを統合した辺の全域木/森」という1つのデータで、可視化（本Phase）と削除時のrebuild（Phase 6）の両方を賄う

## Phase 12: ストレージ（永続化）
当初v1（別リポジトリ）を想定していたが、v0（このリポジトリ）に前倒しする（詳細は冒頭「バージョニング方針」参照）。

- 前提: Phase 5で名前付きDBの層（`Db`、`HashMap<String, Ufdb>`）がすでにある。「1DB = 1ファイル」という単位で素直に永続化に対応させられる
- ~~保存内容の設計判断: `keys`（`HashMap<String, usize>`）と`UnionFind`の`parent`/`size`をそのまま保存するか、`graph`（実際にMERGEされた辺の全域木/森。Phase 11参照）だけを保存して読み込み時に`unite`を再生する形にするかを実装時に決める~~ → **決定**: `keys`と`parent`/`size`をそのまま保存する方式（案A）。読み込みが単純なデシリアライズだけで完結し、10万件規模でも再構築コスト（O(E・α(E))の`unite`再生）が発生しない。トレードオフとして、保存フォーマットが`UnionFind`の内部表現（インデックス割り当て）に縛られる点は許容する。**加えて**、`graph`（Phase 11のSNAPSHOT・Phase 6のUNMERGE rebuildが依存する全域木/森）も保存対象に含める。案A/B の論点は「UnionFindの復元方法」であり`graph`の要否とは別軸で、`graph`を除外するとLOAD後に`SNAPSHOT`/`UNMERGE`が機能しなくなるため。`graph`はインデックスに依存しない`HashMap<String, Vec<String>>`なので、保存対象に加えてもフォーマットの複雑さは増さない
- ~~シリアライズ形式（JSON/bincode等）を実装時に決める~~ → **決定**: JSON（`serde_json`）。このプロジェクトが一貫して選んできた「内部状態を人間が見える形で出す」方向（`SNAPSHOT`のHTML化など）と合う。保存対象の構造体に`serde`の`Serialize`/`Derialize`を実装しておけば、将来10万件規模で速度がボトルネックになった場合でも`bincode`へ低コストで移行できる（フォーマットの差し替えのみで済む）。ただし移行時は既存の`.json`ファイルとの後方互換性（ワンショット移行処理 or 両形式読み込み対応）が別途必要になる点は留意する
- ~~ファイル配置（保存先ディレクトリ、ファイル名の決め方）を実装時に決める~~ → **決定**: PostgreSQLの「固定データディレクトリ（`initdb`で作成、`PGDATA`等で指定、cwdに依存しない）」に倣い、保存先は固定パス`./ufo_data/`（将来的にはPhase 15のconfigファイルで変更可能にする前提。それまでは決め打ち）とする。ファイル名は「1DB = 1ファイル」で`{db_name}.json`をそのまま使う。PostgreSQLはDB名を直接ファイル名にせず内部OID+カタログで間接参照する方式を取っているが、toy_ufdbの規模ではその複雑さは過剰と判断し、代わりに`CREATEDB`実行時にDB名をファイル名として安全な文字のみに制限するバリデーションで対応する
- ~~設計判断ポイント: 保存対象は「現在`USE`中の1DBだけ」か「保持している全DB」か~~ → **決定**: 現在`USE`中の1DBだけ。Phase 5で確立した「UFQLのコマンドは常に現在USEで選択中の1つのDBだけを操作対象にする」という原則（`INSERT`/`MERGE`/`SAME`等と同様）に`SAVE`も合わせる。全DBをまとめて保存したい場合は、各DBに`USE`で切り替えて個別に`SAVE`する運用になる
- ~~起動時に自動で読み込むか、明示的な`LOAD`コマンドでのみ読み込むかを実装時に決める~~ → **決定**: 自動ロード（Redisの起動時RDB自動読み込みに倣う）。プロセス起動時、固定データディレクトリ（前述）内に存在する保存済みファイルをすべて走査し、対応するDB群を`Db`に復元してからREPLを開始する。これは`SAVE`が「現在DBのみを対象にするランタイムコマンド」であるのとは非対称だが、起動時の自動ロードは`USE`操作とは無関係の一括初期化処理であり、「起動直後からできるだけ以前の状態に近づけたい」という自動ロードの目的と「コマンドは常に現在DBのみを操作する」という既存原則は両立する。この設計により明示的な`LOAD`コマンドは不要になる
- [ ] コマンド名: ファイル配置が固定ディレクトリ＋`{db_name}.json`に決まったため、パス引数は不要で`SAVE`（引数なし、現在DBを保存）のみで足りる想定。実装時に最終確認する

## Phase 13: TCPサーバー化
CLIのREPLプロセスと、別プロセスとで同じ「生きているデータ」を共有できるようにする。当初は「Tauri側と共有する必要が出てきたら検討する低優先度事項」としていたが、v0の正式な機能として前倒しする。

- [ ] `Ufdb`（または`Db`）への並行アクセス制御が必要になる（`Arc<Mutex<Ufdb>>`など）。単一プロセス・単一スレッド前提の現状の実装より一段大きな変更になる
- [ ] REPL（標準入出力）とTCPサーバーの関係を決める: 同一プロセスが両方の入り口を持つか、TCPサーバー専用のバイナリに分けるかを実装時に決める
- [ ] プロトコル設計: 現状のUFQLコマンド文字列（`INSERT`/`MERGE`/`SAME`/`GROUPS`等）をそのままTCP越しに1行1コマンドで送受信する形が、REPLとの実装共有という点で自然と思われる（独自バイナリプロトコルは見送る想定）
- 設計判断ポイント（実装しながら決める）:
  - 複数クライアントの同時接続をどこまでサポートするか（まず1接続ずつの逐次処理から始めるのが現実的）
  - Phase 14（ユーザーアカウント）の認証をこのPhaseと同時に作るか、まず認証なしのTCPサーバーを先に動かすかの順序

## Phase 14: ユーザーアカウント（TCP接続時のログイン）
TCPサーバー（Phase 13）に接続するクライアントを認証するためのログインアカウント機能。

- [ ] ユーザー情報（ユーザー名・パスワード等）の管理方法を設計する。Phase 12のストレージ機構を流用して永続化するか、別立てにするかは実装時に決める
- [ ] 認証フロー: 接続直後にユーザー名/パスワードを送らせ、一致すればその後のコマンドを受け付ける、という単純な形を想定
- 設計判断ポイント（実装しながら決める）:
  - 権限モデル: DBごとのアクセス制御を持つか、ログインさえできれば全DBを操作可能にするか（後者の方がv0のシンプルさに合う）
  - パスワードの保存方法（平文は避ける。ハッシュ化の要否・方式は実装時に決める）

## Phase 15: configファイル
起動時に読み込む設定ファイル。TCPサーバーのポート番号、デフォルトDB名、永続化ファイルの保存先など、これまでコード内に決め打ちしていた値を外出しする。

- [ ] フォーマット（TOML/JSON/YAML等）を実装時に決める
- [ ] 設定項目の候補: TCPサーバーのポート番号（Phase 13）、永続化ファイルの保存先ディレクトリ（Phase 12）、デフォルトDB名（現状コード内固定の`"ufdb"`）
- [ ] 設定ファイルが存在しない場合のデフォルト値・挙動を決める
- 位置づけ: Phase 12〜14が実装されて初めて「外出しすべき値」が出揃うため、実装順としては最後が自然

## 検討事項（未定）: ETTによる高速な削除
Phase 6のrebuild方式（O(E・α(E))）の代わりに、削除を**O(log n)**まで高速化できる案。森全体を1本のオイラーツアー系列として平衡二分探索木（splay木など）で持ち、`unite`（link）/削除（cut）/`same`（同じ木か判定）をすべてO(log n)で行う。

- 前提: 今の`unite`は同じグループならno-op（辺を作らない）ので、森に冗長な辺（サイクル）は発生しない。この前提がある限り、素のETTで「特定のMERGEの取り消し＝O(log n)」が実現できる（Phase 11の`graph`がこの前提を満たす具体的な実装になっている）
- 「辺を切った後も別の辺経由でまだ繋がっているか」まで扱う完全動的連結性が必要になった場合は、ETT＋辺のレベル分け（Holm–de Lichtenberg–Thorup算法など）が必要になり、コストもO(log²n)ならしまで上がる。より複雑
- `parent`/`size`の`Vec`2本というシンプルな今の実装を、平衡二分探索木ベースの実装に丸ごと置き換える必要があり、実装コストはPhase 6よりかなり大きい
- 位置づけ: Phase 1やPhase 6には組み込まず、あくまで学習用の検討事項として保留。着手するかは未定

## v1（別リポジトリ）について
永続化・TCPサーバー化・ユーザーアカウント・configファイルはPhase 12〜15としてv0（このリポジトリ）に前倒ししたため、現時点でv1に明確に残っている内容はない。

- 各要素への value 付与 + union 時のマージルール（v0で先にやるか、v1に回すかは未定）
- 今後v0からさらに先の大きな方針転換が必要になった場合に、改めてv1リポジトリの内容を検討する

## GUI（Tauri、別リポジトリ）との連携メモ
- Tauriは別リポジトリで作業する（コアAPIの安定を待たず着手してよい）。Tauri入門も兼ねているため、まずは単一プロセスで進める
- `toy_ufdb`をCargo依存として直接embedし、Tauriアプリ自身のプロセス内で独立した`Ufdb`インスタンスを持つ（CLIとはデータを共有しない）。Phase 13でTCPサーバー化を実装しても、これが本命の構成であることに変わりはない（TCP越しの共有はあくまで選択肢の1つであり、Tauri側に強制するものではない）
- CLIと生きたデータを共有したい、という話が出てきたらPhase 13（TCPサーバー化）を参照
- Phase 11のHTML出力はあくまで静的なスナップショット。動的な更新・編集・インタラクティブな表示はTauriアプリ側の責務であり、この静的HTML出力とは役割が異なる
